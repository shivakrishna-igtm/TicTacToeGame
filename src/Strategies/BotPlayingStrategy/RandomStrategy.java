package Strategies.BotPlayingStrategy;

import models.Board;
import models.Move;
import models.Player;

public class RandomStrategy implements BotPlayingStrategy{

    @Override
    public Move decideMove(Player player, Board board) {
        return null;
    }
}
